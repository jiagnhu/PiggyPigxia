export enum ThemeMode {
  light = 1,
  dark = 2,
}

export const ThemeKey = {
  1: "light",
  2: "dark",
};

export enum SpreadMode {
  spreaded = 1, // 展开
  folded = 2, // 折叠
}

export const NavBarHeight = 56;

export const HomeIdeImgHeight = 244;
